#!/bin/bash	

#####^^^ the above is called a shebang it is important to keep that the same 

####the first word is a command and too uncomment the command delete the hash symbol anything with the hash is a comment and will not be excuted 

who                # displays your username
hostname           #name of the device
ifconfig           #ip address information
uptime             #displays the account of time the computer has been on

# This script below creates a new user through bash

#echo "What will the username be?"

#read username #takes user input stores it as a variable and can be called later on

#useradd (Privalges here rw etc) $username

#echo  " Creating " $username " account now"